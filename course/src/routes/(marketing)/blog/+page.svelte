<svelte:head><title>Blog — Trade Flex</title></svelte:head>

<section class="wrap">
	<h1>Blog</h1>
	<p class="lede">Essays on trading, risk, and the craft of building this platform.</p>
	<p class="note">Posts land here once the CMS ships in a later module. Subscribe to the newsletter
		from the footer and we'll email you when the first post drops.</p>
</section>

<style>
	.wrap {
		max-width: 720px;
		margin: 0 auto;
		padding: var(--space-9) var(--space-6);
	}
	h1 {
		font-size: var(--fs-3xl);
		margin: 0 0 var(--space-3);
	}
	.lede {
		color: var(--color-text-muted);
		font-size: var(--fs-lg);
	}
	.note {
		padding: var(--space-4);
		background: var(--color-surface-2);
		border: 1px dashed var(--color-border);
		border-radius: var(--radius-md);
		color: var(--color-text-muted);
	}
</style>
