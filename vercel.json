{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"buildCommand": "curl -fsSL -o /tmp/pnpm https://github.com/pnpm/pnpm/releases/download/v10.33.0/pnpm-linuxstatic-x64 && chmod +x /tmp/pnpm && cd course && /tmp/pnpm install --frozen-lockfile && /tmp/pnpm build",
	"installCommand": "echo noop — install runs inside course/",
	"outputDirectory": "course/.vercel/output",
	"framework": null,
	"build": {
		"env": {
			"BETTER_AUTH_SECRET": "build-placeholder-thirty-two-chars-min-xxx",
			"ORIGIN": "https://tradeflex.vercel.app",
			"DATABASE_URL": "postgres://build:build@localhost:5432/build"
		}
	}
}
