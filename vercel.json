{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"buildCommand": "corepack enable && corepack prepare pnpm@10.33.0 --activate && cd course && pnpm install --frozen-lockfile && pnpm build",
	"installCommand": "echo noop — install runs inside course/",
	"outputDirectory": "course/.vercel/output",
	"framework": null
}
